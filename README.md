# shiny-sniffle

这是一个 **Spring Boot 项目**，项目原名为 `Springboot_SelfHealthCare`，是一个自监控/健康检查类的后端应用。

## 项目结构
- `src/`: Java 源代码目录
- `pom.xml`: Maven 项目配置文件，定义了 Spring Boot 依赖和构建规则
- `.mvn/`: Maven 构建工具相关文件

## 技术栈
- Java / Spring Boot
- Maven 构建工具
